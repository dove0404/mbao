<view class="goodsList-Box">
  <block wx:for="{{cartGoods}}" wx:key="index">
      <m-cartGoodsItem cartGoodsItem="{{item}}"/>
  </block>
  <m-buttomBar subkey="{{allChecked+totalPrice+totalNum}}"/>
</view>
