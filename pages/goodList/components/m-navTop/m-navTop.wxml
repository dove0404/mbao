<scroll-view class="navTop" scroll-x>
  <block wx:for="{{navTopList}}" wx:key="index">
    <view class="navTop-tittle {{cutrrentIndex == index ? 'active' : ''}}"
          bindtap="handItemClick"
          data-index="{{index}}"
    ><text>{{item}}</text></view>
  </block>
</scroll-view>
